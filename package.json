{
  "name": "nestjs-heros",
  "version": "1.0.0",
  "author": "Joshua Wiens <d3viant0ne>",
  "private": true,
  "license": "Apache-2.0",
  "description": "NestJS microservices mono-repo",
  "scripts": {
    "start": "lerna exec yarn run serve:dev",
    "build": "lerna exec yarn run build:dist",
    "lint": "lerna exec yarn run lint",
    "test": "lerna exec yarn run test",
    "test:coverage": "lerna exec yarn run test:coverage",
    "ci:test:fights": "lerna exec yarn run test --scope fights-service",
    "ci:coverage:fights": "lerna exec yarn run test:coverage --scope fights-service",
    "ci:test:gateway": "lerna exec yarn run test --scope gateway-service",
    "ci:coverage:gateway": "lerna exec yarn run test:coverage --scope gateway-service",
    "ci:test:heros": "lerna exec yarn run test --scope heros-service",
    "ci:coverage:heros": "lerna exec yarn run test:coverage --scope heros-service",
    "ci:test:teams": "lerna exec yarn run test --scope teams-service",
    "ci:coverage:teams": "lerna exec yarn run test:coverage --scope teams-service",
    "ci:test:villains": "lerna exec yarn run test --scope villains-service",
    "ci:coverage:villains": "lerna exec yarn run test:coverage --scope villains-service",
    "ci:lint:commits": "commitlint --from=${CIRCLE_BRANCH} --to=${CIRCLE_SHA1}",
    "ci:release:validate": "commitlint --from=$(git describe --tags --abbrev=0) --to=$(git rev-parse HEAD)",
    "commitlint": "commitlint",
    "commitmsg": "commitlint -e $GIT_PARAMS",
    "lint-staged": "lint-staged"
  },
  "devDependencies": {
    "@commitlint/cli": "^5.2.8",
    "@commitlint/config-conventional": "^5.2.3",
    "codeclimate-test-reporter": "^0.5.0",
    "cross-env": "^5.1.3",
    "del": "^3.0.0",
    "del-cli": "^1.1.0",
    "husky": "^0.14.3",
    "jest": "^22.0.4",
    "lerna": "^2.5.1",
    "lint-staged": "^6.0.0",
    "mongoose-data-seed": "^1.0.2",
    "nsp": "^3.1.0",
    "pre-commit": "^1.2.2",
    "prettier": "^1.9.2",
    "selfsigned": "^1.10.1",
    "typescript": "2.6.2"
  },
  "engines": {
    "node": ">= 8.9.0"
  },
  "repository": "git@github.com:d3viant0ne/nestjs-heros.git",
  "bugs": "https://github.com/d3viant0ne/nestjs-heros/issues",
  "homepage": "https://github.com/d3viant0ne/nestjs-heros#readme",
  "workspaces": [
    "packages/*",
    "services/*"
  ]
}
